# Logs
results/*.log

# Python
__pycache__/
*.pyc

# Published for reproducibility (no raw harmful text):
#   *.py, requirements.txt, README.md
#   data/prompts.jsonl        (sampled public prompts + public labels)
#   data/xstest_prompts.csv   (public benchmark input)
#   data/do_not_answer.jsonl  (public benchmark input)
#   data/responses/*.jsonl    (raw model responses; may contain unsafe text)
#   data/graded/*.jsonl       (grader severity labels only, no text)
#   results/metrics.json, results/escalation_cases.json (aggregate, no text)

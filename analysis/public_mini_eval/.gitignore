# Raw model generations to (public) harmful prompts — do NOT publish.
# These are freshly generated responses and may contain unsafe text.
data/responses/
data/graded/

# Logs
results/*.log

# Python
__pycache__/
*.pyc

# Kept intentionally (safe to publish):
#   *.py, README.md
#   data/prompts.jsonl        (public prompts only)
#   data/xstest_prompts.csv   (public benchmark)
#   data/do_not_answer.jsonl  (public benchmark)
#   results/metrics.json, results/summary.md, results/escalation_cases.json
#     (aggregate labels only, no raw response text)
